services:
  backend:
    image: shopfeshen-backend-deploy:local
    build: ./backend
    container_name: shopfeshen-api
    restart: always
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    environment:
      PORT: ${PORT:-4000}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:8082}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:8082,http://localhost}
      ENABLE_AI_ROUTES: ${ENABLE_AI_ROUTES:-false}
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:4000/api/health').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - app-network

  frontend:
    image: shopfeshen-frontend-deploy:local
    build: ./frontend
    container_name: shopfeshen-web
    restart: always
    ports:  
      - "8082:80"
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
